PORT=5000
NODE_ENV=development

# Database Connection String
# Instruct teammates to replace 'USER', 'PASSWORD', and 'DB_NAME' with their local Postgres details
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/sms_db?schema=public"

# JWT Secrets (Can be shared for dev, but distinct in prod)
JWT_SECRET=school_harmony_dev_secret_key
JWT_EXPIRE=30d

# Frontend URL for CORS
FRONTEND_URL=http://localhost:8080
